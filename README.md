# SSG in C++

This is a very small Blog Website SSG written entirely in C++.
The usage is simple. Write all your blogs in Markdown and place them in `content/`, all the images in `static/images`, and when you want to deploy, just run `make build`

---
More about the project later, it's a WIP
